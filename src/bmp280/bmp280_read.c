/*
 * TODO: Eventually this will connect to and read from the BMP280 sensor
 *       connected to /dev/i2c-1 (the Grove connector device) and run a
 *       TCP/IP server that allows clients to connect and query for sensor
 *       readings.
 */

int main()
{
    // TODO.

    return 0;
}
